<Window x:Class="ProcessGuard.App.Views.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="进程哨兵 · 实时进程监控" Icon="/ProcessGuard;component/Assets/ProcessGuard.ico" Width="1240" Height="830" MinWidth="1080" MinHeight="720" WindowStartupLocation="CenterScreen" Background="#F2F5F9" FontFamily="Microsoft YaHei UI" FontSize="13" Foreground="#243347">
    <Window.Resources>
        <Style TargetType="Button"><Setter Property="Padding" Value="12,7"/><Setter Property="Margin" Value="0,0,6,0"/><Setter Property="Background" Value="#E8EEF5"/><Setter Property="Foreground" Value="#243347"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/></Style>
        <Style TargetType="TextBox"><Setter Property="Padding" Value="10,7"/><Setter Property="BorderBrush" Value="#D5DEE9"/><Setter Property="BorderThickness" Value="1"/><Setter Property="VerticalContentAlignment" Value="Center"/></Style>
        <Style TargetType="CheckBox"><Setter Property="Margin" Value="0,9,0,6"/><Setter Property="VerticalContentAlignment" Value="Center"/></Style>
        <Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#EDF2F7"/><Setter Property="Foreground" Value="#516278"/><Setter Property="Padding" Value="10,12"/><Setter Property="BorderThickness" Value="0"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
        <Style TargetType="DataGridCell"><Setter Property="Padding" Value="10,6"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="DataGridCell"><Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"><ContentPresenter VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter></Style>
        <Style x:Key="Card" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="CornerRadius" Value="10"/><Setter Property="Padding" Value="20,15"/></Style>
    </Window.Resources>
    <Grid Margin="22">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,18">
            <DockPanel DockPanel.Dock="Left">
                <Image Source="/ProcessGuard;component/Assets/ProcessGuard.png" Width="46" Height="46" Margin="0,0,12,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                <StackPanel><TextBlock Text="进程哨兵" FontSize="27" FontWeight="Bold" Foreground="#12243A"/><TextBlock Text="PROCESSGUARD / WINDOWS" Foreground="#6B7C91" Margin="1,3,0,0"/></StackPanel>
            </DockPanel>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center"><TextBlock Text="{Binding RefreshText}" HorizontalAlignment="Right" Foreground="#087E8B"/><TextBlock Text="关闭窗口即停止监控" HorizontalAlignment="Right" Foreground="#6B7C91" Margin="0,5,0,0"/></StackPanel>
        </DockPanel>
        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="1.45*"/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource Card}" Margin="0,0,14,0"><StackPanel><TextBlock Text="整机 CPU" Foreground="#6B7C91"/><TextBlock Text="{Binding CpuText}" FontSize="30" FontWeight="SemiBold" Margin="0,3,0,8"/><ProgressBar Value="{Binding CpuValue, Mode=OneWay}" Maximum="100" Height="5" BorderThickness="0" Foreground="#0891A2" Background="#E4F2F4"/><TextBlock Text="全部逻辑处理器总算力" Foreground="#6B7C91" Margin="0,8,0,0" FontSize="11"/></StackPanel></Border>
            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="0,0,14,0"><StackPanel><TextBlock Text="整机内存" Foreground="#6B7C91"/><TextBlock Text="{Binding MemoryText}" FontSize="30" FontWeight="SemiBold" Margin="0,3,0,8"/><ProgressBar Value="{Binding MemoryValue, Mode=OneWay}" Maximum="100" Height="5" BorderThickness="0" Foreground="#6375D8" Background="#EBEDFA"/><TextBlock Text="{Binding PhysicalMemoryText}" Foreground="#6B7C91" Margin="0,8,0,0" FontSize="11"/></StackPanel></Border>
            <Border Grid.Column="2" Style="{StaticResource Card}" Background="#132D43"><StackPanel><TextBlock Text="自动结束控制" Foreground="#9DB5C7"/><CheckBox Content="开启自动结束" IsChecked="{Binding AutoTerminate}" Foreground="White" FontWeight="SemiBold" FontSize="17" AutomationProperties.AutomationId="AutoTerminate"/><TextBlock Text="{Binding AutoStatus}" Foreground="#81DBD3" Margin="0,1,0,6"/><TextBlock Text="强制结束可能丢失未保存的数据。" Foreground="#D1DCE4" FontSize="11"/></StackPanel></Border>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="296"/></Grid.ColumnDefinitions>
            <Border Background="White" CornerRadius="10" Margin="0,0,14,0" Padding="0,16,0,0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <DockPanel Margin="16,0,16,12"><TextBlock Text="进程列表" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center"/><TextBlock Text="{Binding ProcessCount}" Foreground="#6B7C91" HorizontalAlignment="Right" VerticalAlignment="Center"/></DockPanel>
                    <TextBox Grid.Row="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="16,0,16,12" ToolTip="按名称或 PID 搜索，仅过滤显示" AutomationProperties.Name="搜索进程" AutomationProperties.AutomationId="Search"/>
                    <DataGrid Grid.Row="2" x:Name="ProcessGrid" ItemsSource="{Binding Processes}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F3F7" BorderThickness="0" RowHeaderWidth="0" Background="White" AlternatingRowBackground="#FAFCFE" EnableRowVirtualization="True" EnableColumnVirtualization="True" SelectionMode="Single" Sorting="OnSort">
                        <DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="ToolTip" Value="{Binding Error}"/></Style></DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="进程名称" Binding="{Binding Name}" Width="160" SortMemberPath="Name"/>
                            <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70" SortMemberPath="Pid"/>
                            <DataGridTextColumn Header="CPU" Binding="{Binding CpuText}" Width="72" SortMemberPath="CpuPercent"/>
                            <DataGridTextColumn Header="内存 MB" Binding="{Binding MemoryText}" Width="95" SortMemberPath="MemoryMb"/>
                            <DataGridTextColumn Header="内存 %" Binding="{Binding MemoryPercentText}" Width="82" SortMemberPath="MemoryPercent"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="175" SortMemberPath="Status"/>
                            <DataGridTextColumn Header="连续超限" Binding="{Binding Duration}" Width="150" CanUserSort="False"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="18,16"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                <TextBlock Text="规则与保护" FontSize="17" FontWeight="SemiBold"/>
                <TextBlock Text="单进程 CPU 或内存 &gt; 80%&#10;分别连续超过 10 秒才结束" TextWrapping="Wrap" Foreground="#516278" LineHeight="23" Margin="0,10,0,0"/>
                <TextBlock Text="内存 % = 工作集 / 整机物理内存" FontSize="11" Foreground="#6B7C91" Margin="0,5,0,0"/>
                <CheckBox Content="保护 explorer.exe" IsChecked="{Binding ProtectExplorer}"/>
                <CheckBox Content="当前用户登录时启动" IsChecked="{Binding StartupEnabled}" AutomationProperties.AutomationId="Startup"/>
                <Separator Margin="0,10,0,14" Background="#E4EAF1"/>
                <TextBlock Text="白名单 · 始终保护" FontWeight="SemiBold" Foreground="#087E8B"/>
                <TextBox x:Name="WhiteInput" Margin="0,9,0,7" ToolTip="例如 app.exe" AutomationProperties.AutomationId="WhiteInput"/>
                <StackPanel Orientation="Horizontal"><Button Content="添加" Click="AddWhite"/><Button Content="移除选中" Click="RemoveWhite"/></StackPanel>
                <ListBox x:Name="WhiteList" ItemsSource="{Binding Whitelist}" Height="74" Margin="0,8,0,14" BorderBrush="#E4EAF1"/>
                <TextBlock Text="黑名单 · 优先监控" FontWeight="SemiBold" Foreground="#AB6530"/>
                <TextBox x:Name="BlackInput" Margin="0,9,0,7" ToolTip="例如 app.exe" AutomationProperties.AutomationId="BlackInput"/>
                <StackPanel Orientation="Horizontal"><Button Content="添加" Click="AddBlack"/><Button Content="移除选中" Click="RemoveBlack"/></StackPanel>
                <ListBox x:Name="BlackList" ItemsSource="{Binding Blacklist}" Height="74" Margin="0,8,0,10" BorderBrush="#E4EAF1"/>
                <TextBlock Text="按文件名匹配全部同名进程。白名单优先；黑名单不绕过系统保护或触发阈值。" TextWrapping="Wrap" FontSize="11" Foreground="#6B7C91" LineHeight="18"/>
                <Button Content="打开日志目录" Click="OpenLogs" Margin="0,14,0,0"/>
            </StackPanel></ScrollViewer></Border>
        </Grid>
        <StackPanel Grid.Row="3" Margin="2,12,0,0"><TextBlock Text="{Binding LastAction}" TextTrimming="CharacterEllipsis" Foreground="#516278"/><TextBlock Text="{Binding Error}" Foreground="#B64236" TextWrapping="Wrap" Margin="0,4,0,0"/></StackPanel>
    </Grid>
</Window>
